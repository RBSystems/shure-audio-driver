package main

import "github.com/byuoitav/shure-audio-library/shure"

func readEvents() error {

	// go through all shure devices in room
	// for each one start a go routine that waits for events to come in from that device
	// push events to event hub

	return nil
}

func waitForEvents(conn *shure.Connection) error {

}
